# Claude Code Hooks

This directory will hold Claude Code-specific hook assets for bootstrap, continuation, and runtime overlays.

Hook code belongs here rather than in the repo root so distributable behavior stays inside the template layer.
