# Internal Runtime Template Install

This document will become the installation guide for the internal runtime template.

## Current Install Strategy

Start from the OpenCode-compatible path and add only the minimum internal overlay required by the actual runtime.

## Planned Surfaces

- internal plugin or config registration
- adapter-specific bootstrap connection
- runtime verification checklist

## Verification Goal

After installation, the internal runtime should:

- load the entry guidance
- activate bootstrap behavior
- classify the first actionable request through the shared intake contract

## Status

This guide is intentionally incomplete until the internal runtime surfaces are confirmed.
